  origin: custo-traducao @ v0.07.00-37-g86ae4d96  |  dskhmartins Linux 7.0.0-34-generic  |  gcc 15.2.0  |  2026-09-26T00:39:10-0300

Address translation cost (sequential walk over 32 MB)
(21 samples per measurement; times in ns)

  measurement                           median  p25-p75 (IQR)   range min-max      disp    CV
  ---------------------------------- ---------  --------------- ----------------- ----- -----
  4 KB pages                              1.20  1.20-1.20       1.19-1.21           0.5%   0.5%  
  2 MB hugepages                          1.14  1.14-1.15       1.13-1.17           1.1%   0.9%  

  The difference below is PAIRED -- delta_i = t_4k,i - t_2m,i in the same
  loop iteration -- and therefore has a distribution of its own:

  DIFFERENCE attributable to translation     0.052  IQR 0.049 to 0.058   range 0.030 to 0.078   21/21 pairs

  page walk cost: 0.05 ns  (4.3% of the 4 KB access)
  the last column is what supports the conclusion: in 21 of 21 pairs the
  4 KB page was the slower one. Subtracting two medians does not say that.

  RAM latency (~1 ns) appears in both measurements and does not
  depend on page size. The difference above is the cost of the
  page walk, which hugepages remove: 0.1% of the budget of
  67.2 ns per packet at 10 GbE, spent before any useful work.
