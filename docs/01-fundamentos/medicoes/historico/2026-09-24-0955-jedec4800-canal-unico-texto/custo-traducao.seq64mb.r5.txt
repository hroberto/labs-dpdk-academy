  origin: custo-traducao @ v0.06.00-23-g6b7d70e  |  dskhmartins Linux 7.0.0-31-generic  |  gcc 15.2.0  |  2026-09-24T10:50:01-0300

Address translation cost (sequential walk over 64 MB)
(21 samples per measurement; times in ns)

  measurement                           median  p25-p75 (IQR)   range min-max      disp    CV
  ---------------------------------- ---------  --------------- ----------------- ----- -----
  4 KB pages                              2.29  2.20-2.30       1.92-2.36           4.3%   5.1% ~
  2 MB hugepages                          2.30  2.27-2.33       2.23-2.42           2.7%   2.0%  

  The difference below is PAIRED -- delta_i = t_4k,i - t_2m,i in the same
  loop iteration -- and therefore has a distribution of its own:

  DIFFERENCE attributable to translation    -0.049  IQR -0.106 to -0.010   range -0.329 to 0.063   17/21 pairs

  page walk cost: -0.05 ns  (-2.1% of the 4 KB access)
  the last column is what supports the conclusion: in 17 of 21 pairs the
  4 KB page was the slower one. Subtracting two medians does not say that.

  RAM latency (~2 ns) appears in both measurements and does not
  depend on page size. The difference above is the cost of the
  page walk, which hugepages remove: -0.1% of the budget of
  67.2 ns per packet at 10 GbE, spent before any useful work.
