  origin: custo-traducao @ v0.07.00-25-g836c47a  |  dskhmartins Linux 7.0.0-34-generic  |  gcc 15.2.0  |  2026-09-25T16:41:03-0300

Address translation cost (scattered walk over 16 MB)
(21 samples per measurement; times in ns)

  measurement                           median  p25-p75 (IQR)   range min-max      disp    CV
  ---------------------------------- ---------  --------------- ----------------- ----- -----
  4 KB pages                             10.31  10.25-10.34     10.16-14.66         0.9%   9.4%  
  2 MB hugepages                          8.61  8.61-8.62       8.61-8.62           0.1%   0.0%  

  The difference below is PAIRED -- delta_i = t_4k,i - t_2m,i in the same
  loop iteration -- and therefore has a distribution of its own:

  DIFFERENCE attributable to translation      1.69  IQR 1.63 to 1.72   range 1.54 to 6.05   21/21 pairs

  page walk cost: 1.69 ns  (16.4% of the 4 KB access)
  the last column is what supports the conclusion: in 21 of 21 pairs the
  4 KB page was the slower one. Subtracting two medians does not say that.

  RAM latency (~9 ns) appears in both measurements and does not
  depend on page size. The difference above is the cost of the
  page walk, which hugepages remove: 2.5% of the budget of
  67.2 ns per packet at 10 GbE, spent before any useful work.
